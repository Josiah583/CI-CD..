name: testing pipeline

on:
  push:
    branches:
        - master

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      -
        name: step1
        run: echo "say hello world. this is our first "